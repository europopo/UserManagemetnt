import { CanflyingPurePipe } from './canflying-pure.pipe';

describe('CanflyingPurePipe', () => {
  it('create an instance', () => {
    const pipe = new CanflyingPurePipe();
    expect(pipe).toBeTruthy();
  });
});
